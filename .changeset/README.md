# Changesets

This directory contains changeset files for versioning and changelog generation.

Write changeset files by hand — do not run `pnpm changeset`.

See the [changelog guide](../README.md) for format details.
